# 五邑大学天翼校园自动登录工具

> **支持锁屏界面自动连接网络——赶早八再也不用输入锁屏密码=v=**
> 断网后自动检测连接情况，重启网卡 / 重启软件，连上为止。

![Platform](https://img.shields.io/badge/平台-Windows%2010%2F11-blue)
![PowerShell](https://img.shields.io/badge/PowerShell-5.1%2B-5391FE)
![License](https://img.shields.io/badge/License-MIT-green)
![学校](https://img.shields.io/badge/五邑大学-天翼校园-red)

天翼校园客户端（广东电信 GDPortal / ESurfingClient）用起来有几个老毛病：开机要手动点"登录"、断网后要重新点、有时候登录按钮还出不来得禁用再启用网卡。本工具用一个小巧的 PowerShell 看门狗把这些操作全部自动化——装上以后你再也不用碰那个客户端。

---

## ✨ 功能特性

- **开机自动登录**：开机进桌面后自动检测网络，检测到没登录会自动帮你点"登 录"，几十秒内自动上网
- **断网自动恢复**：每 20 秒探测一次外网，断网自动重启客户端并点击登录，一直重试到连上为止
- **锁屏也能连**：采用后台窗口消息点击（不抢鼠标、不需要窗口在前台），**电脑锁屏状态下同样有效**——晚上断网、早上起来网已经连好了，锁屏界面直接就能上网，不用输密码进桌面点登录
- **登录按钮卡死也能救**：点击无效时自动"禁用→启用水网卡"（对应手动操作），再重试登录，带 3 分钟冷却防止反复断网
- **全程自动、无人值守**：人在宿舍还是教室都一样；断网恢复时会弹 Windows 通知告诉你
- **零依赖**：只用 Windows 自带 PowerShell，不装任何第三方软件、不修改客户端程序、不接触你的账号密码（账号密码始终只存在官方客户端里）
- **自带日志与排障工具**：所有动作都有日志，点击前自动截图存档，客户端界面改版导致点不中时可快速重新校准坐标

## 📋 适用环境

- 五邑大学 校园网（广东电信天翼校园客户端，客户端界面为 533×914 标准窗口）
- Windows 10 / 11，有线连接（宿舍网口）
- 需要管理员权限安装（注册开机自启的计划任务、控制网卡）

> 也适用于其他使用同款"天翼校园客户端（广东电信 GDPortal）"的高校，只要客户端界面一致即可，按钮坐标可在配置里微调。

## 🚀 快速开始

1. 到本仓库 [Releases](../../releases) 页面下载最新的 `WYU-ESurfing-Auto-Login-v1.0.0.zip`（一键分享包）
2. 把压缩包解压到任意目录（**不要直接在压缩包里运行**）
3. 保持天翼校园客户端处于已登录过一次的状态（账号、密码已记住），双击 **`一键安装.bat`**
4. 在 UAC 弹窗点"是"→ 安装程序会自动找到客户端位置和上网网卡 → 注册开机自启 → 启动看门狗
5. 看到 `安装完成!` 即大功告成，之后无需任何手动操作

卸载：双击 `卸载.bat`（或在安装包里运行 `install.ps1 -Uninstall`）。

## 🔧 工作原理

天翼校园客户端的登录界面是 miniblink 内嵌网页（窗口类 `wkeWebWindow`），没有标准 UI 控件，因此本工具采用**窗口坐标点击**：

```
每 20 秒 TCP 探活 (223.5.5.5:443 / 119.29.29.29:53 / 114.114.114.114:53)
        │
        ├─ 通 ─→ 继续监测
        │
        └─ 连续 2 次不通 ─→ 判定断网, 开始恢复梯子
                 │
                 ├─ 第 1 级: 重启客户端 → 后台点击"登 录"(PostMessage, 不抢鼠标)
                 │             └─ 成功? → 通知"已恢复"
                 │
                 └─ 第 2 级: 禁用/启用水网卡 (冷却 180 秒) → 重启客户端 → 再点登录
                              └─ 最多耐心等 75 秒让认证生效, 失败则稍后重试
```

- **后台点击**：向客户端窗口直接发送鼠标消息，不需要窗口在前台、不移动你的鼠标，**锁屏状态同样生效**；若无效会退回"前台真实点击"（需要解锁状态）
- **坐标**：登录页"登 录"按钮在窗口内固定位置（默认 `267, 533`，对应 533×914 标准窗口）；已连接页面的"断 开"按钮在 `267, 610`，两者不同，工具只会在断网时点登录按钮
- **不发一个网络包给认证服务器**：所有登录动作都由官方客户端自己完成，工具只负责"帮你点一下按钮"和"帮你重启一下网卡"

## ⚙️ 配置说明

安装后配置在 `%LOCALAPPDATA%\CampusNetWatchdog\config.json`，修改后重启看门狗生效：

```powershell
Stop-ScheduledTask -TaskName CampusNetWatchdog; Start-ScheduledTask -TaskName CampusNetWatchdog
```

| 配置项 | 默认 | 说明 |
|---|---|---|
| `client_dir` | 自动探测 | 天翼校园客户端安装目录 |
| `adapter_name` | 自动探测 | 上网用的网卡名称（"网络连接"里看到的名称） |
| `login_btn_x` / `login_btn_y` | 267 / 533 | "登 录"按钮在客户端窗口内的坐标 |
| `check_interval_sec` | 20 | 探活间隔（秒） |
| `fail_threshold` | 2 | 连续失败几次判定断网 |
| `nic_bounce_cooldown_sec` | 180 | 两次网卡重启最小间隔（秒） |
| `auth_settle_wait_sec` | 75 | 点击登录后最长等待认证时间（秒） |

## 🕹 命令通道（进阶）

在 `%LOCALAPPDATA%\CampusNetWatchdog\` 下新建 `command.txt`，写入命令保存，看门狗下一轮（≤20 秒）执行并自动删除该文件：

| 命令 | 作用 |
|---|---|
| `status` | 把当前在线状态 / 网卡 / 客户端进程写进日志 |
| `bounce` | 立即重启一次网卡 |
| `shot` | 弹出客户端窗口截图存档后自动收回托盘 |
| `click x y` | 向客户端窗口发送一次后台点击（如 `click 267 610` 点"断开"） |

调试探活：`powershell -File watchdog.ps1 -Check`（只探一次并输出状态）。

## ❓ 常见问题

**Q: 电脑锁屏 / 睡眠时能用吗？**
锁屏可以：后台点击不依赖前台焦点。**睡眠不行**——睡眠时程序不运行，且校园网会话通常已超时，唤醒后看门狗会在 1 分钟内自动重新登录。

**Q: 客户端升级后界面变了、点不中按钮怎么办？**
日志目录里有每次点击前的截图 `shot-*.png`，或运行 `tools\uia-probe.ps1` 重新截图，量出"登 录"按钮中心坐标，改 `config.json` 里的 `login_btn_x/login_btn_y` 即可。

**Q: 会不会和手动操作冲突？**
不会。工具只在"探测到断网"时才动作；你手动连上后它就安静监测。

**Q: 杀毒软件报毒？**
脚本是明文的 PowerShell（可直接用记事本查看），未加壳未混淆，行为只有：TCP 探活、窗口消息点击、禁用/启用网卡。若被拦截请放行，或把 `%LOCALAPPDATA%\CampusNetWatchdog` 加入信任目录。

**Q: 为什么不用 HTTP 直接模拟登录？**
学校认证门户（`zsteduapp.10000.gd.cn`）有瑞数反爬加密，且账号密码只在客户端内加密存储；模拟点击官方客户端是更稳定、更安全的做法。

## 📁 目录结构

```
.
├── 一键安装.bat          # 双击安装 (自动提权)
├── 卸载.bat              # 双击卸载
├── install.ps1           # 安装/卸载逻辑 (自动探测 + 写配置 + 注册计划任务)
├── watchdog.ps1          # 看门狗主程序 (常驻循环)
├── config.example.json   # 配置示例
├── tools/
│   └── uia-probe.ps1     # 窗口探测/按钮坐标测量工具 (调试用)
└── LICENSE
```

安装后实际运行在 `%LOCALAPPDATA%\CampusNetWatchdog\`（含 `config.json`、`logs\`），计划任务名 `CampusNetWatchdog`（登录时自启、最高权限、无运行时限）。

## 🗑 卸载

双击 `卸载.bat`，或手动：

```powershell
Stop-ScheduledTask -TaskName CampusNetWatchdog -ErrorAction SilentlyContinue
Unregister-ScheduledTask -TaskName CampusNetWatchdog -Confirm:$false -ErrorAction SilentlyContinue
Remove-Item -Recurse "$env:LOCALAPPDATA\CampusNetWatchdog"
```

## ⚠️ 免责声明

- 本工具仅自动化你本人账号在**官方客户端**上的点击操作，不破解、不篡改认证协议，不获取、不存储你的账号密码
- 请在遵守学校网络管理规定的前提下使用；因使用本工具产生的一切后果由使用者自行承担

## 📄 License

[MIT](LICENSE) © st-cx
